'use strict';

const boardsApi = require('../api/boards-api');

module.exports = (post)=> {
    // code also will be in this place.
    // think how to structure route architecture. it will be not same as api!
    // write some code in routes.
    // api will have methods which can affect only on model state.
    // routes may have different functional.
});
