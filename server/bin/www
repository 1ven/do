#!/usr/bin/env node

const app = require('../');
const config = require('../config');
const postgres = require('../db/postgres');

postgres.sequelize.sync().then(() => {
    app.listen(config.port, () => console.log(`Listening on ${config.port}...`));
});
