// import chai, { assert } from 'chai';
// import chaiaspromised from 'chai-as-promised';
// import { recreateTables } from '../helpers';
// import db from 'server/db';
// import User from 'server/models/User';

// chai.use(chaiAsPromised);

// describe('User model', () => {
//     beforeEach(recreateTables);

//     // describe('add', () => {
//     //     it('should not create user, when provided username is containing spaces', () => {
//     //         return User.add({
//     //             username: 'i am john',
//     //             email: 'test@mail.com',
//     //             password: '123456'
//     //         }).then(() => {});
//     //     });

//         // it('should not create user, when provided username is more than 20 characters length');

//         // it('should not create user, when provided username is less than 3 characters length');

//         // it('should convert username into lowercase');

//         // it('should not create user, when user with provided username is exists');

//         // it('should not create user, when provided email is not valid');

//         // it('should not create user, when provided email already exists');
//     });
// });
